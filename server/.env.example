# Database
DATABASE_URL=postgresql://user:password@host/database

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server
PORT=5000
NODE_ENV=development

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Email Configuration (for verification emails)
EMAIL_USER=noreply@polithane.com
EMAIL_PASSWORD=your-gmail-app-password

# Email Service Provider Options:
# 1. Gmail: Use "gmail" as service, set EMAIL_USER to your gmail, EMAIL_PASSWORD to app password
# 2. SendGrid: Set SENDGRID_API_KEY
# 3. AWS SES: Set AWS credentials
# 4. Custom SMTP: Set SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD

# Note: For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" from Google Account settings
# 3. Use that app password as EMAIL_PASSWORD
